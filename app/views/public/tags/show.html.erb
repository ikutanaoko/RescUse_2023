
  <div><h5 class="font-weight-bold">タグから探す</h5></div>
  <div>
  <% @tags.each do |tag| %>
    <i class="fas fa-tag pl-2"></i><%= link_to tag.name, tag_path(tag) %>(<%= tag.products.count %>)
  <% end %>  
<div class="row">  
  </div>
  <%= render "public/products/index", products: @products %>

  <div class="text-center mx-auto">
  <hr>
    <%= paginate @products %>
  </div>

</diV>